import { api, apiHelper } from "./axios";
import { config } from "./config";

export const departmentsApi = {
    // Get all users with pagination and filters
    getDepartments: () => {
        return api.get("dasdsadsa");
    },

};
